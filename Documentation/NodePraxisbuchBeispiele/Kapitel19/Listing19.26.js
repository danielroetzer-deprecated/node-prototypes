function example(input) {
    var i;
    if (input.length < 2) {
        throw new Error('Too few characters');
    }

    for (i = 0; i < 10; i++) {
        output += i + '. Output: ' + input;
    }

    var output

    return output;
    output += 'result';
}
